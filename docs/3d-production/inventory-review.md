# 协作资料复核

复核日期：2026-09-08。主 Agent 独立记录，不覆盖 `inventory/` 原始交付。复核期间原稿仍有更新，本记录仅表示本轮读取时的状态。

## 接收结论

五份文件齐全，内容分类与源码索引可用于继续查找资料；**暂不能作为已验收的批量建模输入或完整 3D 验收清单**。以下修正不会阻断主线开发，也不触发代码重构或素材重命名。

本次独立核实：4 种酒馆、4 套牌桌、8 位对手、19 项物品、16 条酒馆专属路线；素材为 33 张场景图、8 个视频、9 个 BGM。CSV 的 40 个素材组路径均存在，覆盖全部 33 张场景图。三处牌组均为 56 张，文件内容的 SHA-256 多重集合一致。未重新逐张查看全部参考图、播放全部音视频或作授权法律判断。

## 需要修正的具体问题

| 项目 | 复核结果与证据 | 处理建议 |
|---|---|---|
| CSV 可解析性 | 表头 11 列，部分记录为 10、12 或 13 列；未转义的英文逗号与缺失尾列会使字段错位。初次复核发现 15 行异常 | 用标准 CSV 写入器重写，再逐行检查列数和数量；保持原始内容可追溯 |
| 3D 项目状态 | `assets.csv` GD-008 将新主场景写为历史归档。`Godot/project.godot:14` 已将其设为运行入口；`three_d/scripts/world.gd` 实际实例化 stash.glb | 网页未引用不等于 Godot 未集成；以当前引擎入口判断。GLB 实测 11,705,068 字节，约 11.7 MB |
| AI 参数调用 | `content-map.md:188` 称未发现 patternPunish 调用；实际 `src/ai.js:12` 参与 patternPressure，`:125` 还会调整其值 | 删除“可能未接入”的判断；是否需要调优另行测试 |
| 通用路线图引用 | 四张 route-general/fixed/dropbag 图在 `scenes/extraction/style.css:166` 起分别引用；`scenes/extraction/index.js:190` 起映射类型到 CSS 类 | 改为已有源码引用证据，不再只按 data.js 查找；引用不等于已实跑 |
| 直升机坪图片路径 | `content-map.md:122` 的 tavern-rooftop-club-route-heliport-bg.png 不存在；实际有 tavern-high-rise-suite-route-heliport-bg.png | 保留命名差异为待确认，不把不存在的路径当作屋顶路线参考绑定 |
| 固定撤离计费 | `content-map.md:138` 写 finalCost 从预约费用抵扣；`src/game.js:838` 扣预约费，`:1024` 再扣 finalCost | 应说明为分两次支付，不能按抵扣迁移 |
| 撤离失败预期 | AC-EXTRACT-002 将热度封锁或现金不足等同于立即被抓结算；`src/game.js:987–999` 对普通出口直接拒绝并返回 | 区分“无法使用此出口”和实际失败结算，按具体路径编写 |
| 3D QA 范围 | 验收清单开头定位为浏览器版；AC-PHYS-001/002 用 N/A 或待启用，未给出出生点、走动碰撞的可操作步骤 | 按原任务书补成真正的 3D 操作清单，所有未运行项保持未执行；逐项关联房间、入口和证据 |
| 数量与文案一致性 | QA 实测 170 个 AC 行，摘要写 167；许可文件视频写 7 个却列出 8 个；marked-lens 的模型建议出现明显错误词语 | 统一以实测统计生成摘要，校对物品用途，避免将笔误带入建模需求 |
| 许可证据表述 | CSV 出现 Byvecade License，但本地 LICENSE 标题为 MIT；README 声明原始素材来源为 public domain | 按本地原文记录，删去无证据名称。未发现第三方来源记录不等于已证明侵权；也不能据此替自制资产新增对外授权 |

## 主线采用方式

- 采用经源码核对的内容 ID 和数量作为索引，模型任务仍逐项打开参考图确认。
- 保留“路线配置键”和“路线实际 ID”两个字段：前者决定规则分支，后者区分酒馆具体出口，二者不同本身不是缺陷；本轮不统一重命名。
- 当前制作顺序不变：先完成藏匿点、烟雾酒馆和前两桌的可玩样板，达到质量后再扩展其他环境。资料中的优先级和建议不自动替代用户确认的范围。
- 新的资金和撤离功能以 `run-migration.md`、对应自动测试及实际引擎截图为验证依据。协作清单尚未执行的功能不标为通过。

原稿保持原位，未在本次主 Agent 提交中纳入；本文件可交回协作 agent 用于定向修订。

## 2026-09-08 晚间复验

协作稿已修正 patternPunish 调用和预约费用不抵扣等关键说明。再次以标准 CSV 解析，40 条记录中仍有第 39、41 行只有 10 列（表头 11 列）；GD-008 的用途说明已更新，但分类还保留“历史归档”。作为资料索引可继续使用，作为批量建模输入前仍需消除这些字段歧义。原稿继续保留，不把资料修订当作游戏实跑通过。
